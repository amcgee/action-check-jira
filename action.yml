name: "DHIS2 Jira Automation"
description: "Enforces DHIS2 GitHub-Jira linking rules and adds convenient PR comments"
inputs:
  GITHUB_TOKEN:
    required: true
    description: "The GitHub token to use for posting comments"
runs:
  using: "node16"
  main: "dist/index.js"
